---
interface Props {
	as?: keyof HTMLElementTagNameMap;
	class?: string;
}

const { as: Tag = "div", class: className } = Astro.props;
---

<Tag
	class:list={[
		"mx-auto w-full max-w-[1200px]",
		"px-6 md:px-16 lg:px-[120px]",
		className,
	]}
>
	<slot />
</Tag>
