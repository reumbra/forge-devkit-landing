---
import DocsLayout from "../../layouts/DocsLayout.astro";
import DocHighlight from "@shared/ui/DocHighlight.astro";

const coreModules = [
	{
		id: "core",
		name: "forge-core",
		tier: "Starter+",
		tagline: "The foundation. One command makes your AI understand your entire project.",
		description: "forge-core runs a 7-gate architecture audit that auto-detects your stack, layers, conventions, and patterns. It generates project-specific dev-skills and 50+ quality guardrails that your AI follows in every session. The universal Hub classifies your intent, walks the dependency graph, and routes to the right pipeline - so you never need to memorize commands or worry about missing prerequisites.",
		benefits: [
			"Universal Hub: describe what you want in plain English - no commands to memorize, no prerequisites to miss",
			"Auto-detects stack, architecture patterns, and conventions",
			"Generates 50+ quality patterns tailored to your project",
			"Creates project-specific dev-skills your AI loads automatically",
			"Scoped pipeline phases prevent AI from going off-track",
			"15 LLM rationalization detectors catch AI shortcuts",
		],
		commands: [
			{
				command: "/forge:setup",
				description: "Setup wizard, ecosystem dashboard, upgrade, and reconfigure.",
				subcommands: [
					{ sub: "/forge:setup", detail: "Run the 7-gate wizard (first time) or show the ecosystem dashboard (after setup)" },
					{ sub: "/forge:setup upgrade --check", detail: "Check for plugin updates" },
					{ sub: "/forge:setup upgrade", detail: "Apply available updates" },
					{ sub: "/forge:setup reconfigure", detail: "Re-run specific gates without full setup" },
				],
			},
			{
				command: "/forge:hub",
				description: "Universal entry point - classifies intent, resolves prerequisites, executes the right pipeline.",
				subcommands: [
					{ sub: '/forge:hub "add payment system"', detail: "Describe what you want - Hub classifies intent, walks dependency graph, builds execution chain" },
					{ sub: '/forge:hub "fix login bug"', detail: "Direct routing - bug fixes skip prerequisites and go straight to /dev" },
					{ sub: '/forge:hub "run SEO audit"', detail: "Non-dev intents route to the right module without touching the dev pipeline" },
				],
			},
			{
				command: "/forge:patch",
				description: "Quick config patches without running the full wizard.",
				subcommands: [
					{ sub: "/forge:patch", detail: "Interactive patch mode - select what to adjust" },
				],
			},
			{
				command: "/dev",
				description: "Start a development task using the generated pipeline. Hub routes here automatically for implementation work.",
				subcommands: [
					{ sub: "/dev", detail: "Begin a feature/fix using your project's dev-skills and quality patterns" },
					{ sub: "/forge:sync", detail: "Sync generated artifacts after architecture changes" },
					{ sub: "/forge:close", detail: "Finalize and close the current development task" },
				],
			},
		],
		workflow: "Install \u2192 /forge:setup (7-gate audit) \u2192 /forge:hub to start any task. Hub resolves prerequisites and routes to /dev, /forge:qa, or other modules automatically.",
	},
	{
		id: "product",
		name: "forge-product",
		tier: "Pro+",
		tagline: "Describe features in business language. AI builds from spec, not guesswork.",
		description: "forge-product bridges the gap between what you want and what AI builds. Describe a feature in plain language - it generates user flows, user stories with acceptance criteria, and UX specifications. These artifacts feed directly into the development pipeline, so your AI codes from verified requirements instead of hallucinated assumptions.",
		benefits: [
			"9 product archetypes (SaaS, marketplace, API platform, etc.)",
			"Generates user flows, stories, and acceptance criteria automatically",
			"50+ UX criteria library for consistent quality",
			"Stories feed directly into /dev pipeline - zero copy-paste",
			"Supports multiple methodologies (Lean, Jobs-to-be-Done, etc.)",
		],
		commands: [
			{
				command: "/forge:product-setup",
				description: "Configure product design module - select archetype, methodology, integrations.",
				subcommands: [
					{ sub: "/forge:product-setup", detail: "Interactive setup wizard for product module" },
				],
			},
			{
				command: "/forge:product",
				description: "Product design operations - features, epics, roadmap, strategy.",
				subcommands: [
					{ sub: "/forge:product design", detail: "Design a feature with user flows, stories, and acceptance criteria" },
					{ sub: "/forge:product epic", detail: "Create an epic with breakdown into stories" },
					{ sub: "/forge:product roadmap", detail: "Generate or update a product roadmap" },
					{ sub: "/forge:product strategy", detail: "Define or refine product strategy (positioning, ICP, GTM)" },
				],
			},
		],
		workflow: "/forge:product-setup \u2192 /forge:product design for each feature \u2192 stories feed into /dev pipeline.",
	},
	{
		id: "qa",
		name: "forge-qa",
		tier: "Pro+",
		tagline: "Tests that trace back to requirements. Not AI guesswork.",
		description: "forge-qa generates tests from acceptance criteria, not from AI assumptions. It supports 10 testing modes and maintains a 4-level traceability matrix: acceptance criteria \u2192 unit tests, use cases \u2192 E2E tests, UX criteria \u2192 visual tests, and LLM-as-Judge for AI output quality. Every test proves a requirement - no orphan tests, no missing coverage.",
		benefits: [
			"Generates tests from acceptance criteria - not guesswork",
			"4-level traceability: AC \u2192 unit, UC \u2192 E2E, UX \u2192 visual, Judge",
			"10 testing modes for different scenarios",
			"LLM-as-Judge evaluates AI-generated output quality",
			"Quality gates block merges until requirements are covered",
		],
		commands: [
			{
				command: "/forge:qa-setup",
				description: "Configure QA module - select test frameworks, coverage thresholds, traceability level.",
				subcommands: [
					{ sub: "/forge:qa-setup", detail: "Interactive QA configuration wizard" },
				],
			},
			{
				command: "/forge:qa",
				description: "Test operations - generation, coverage analysis, quality gates.",
				subcommands: [
					{ sub: "/forge:qa generate", detail: "Generate tests from acceptance criteria or user stories" },
					{ sub: "/forge:qa coverage", detail: "Analyze test coverage and suggest missing tests" },
					{ sub: "/forge:qa trace", detail: "Show traceability matrix (AC \u2192 test \u2192 code)" },
					{ sub: "/forge:qa judge", detail: "Run LLM-as-Judge to evaluate test quality" },
					{ sub: "/forge:qa gate", detail: "Run quality gate checks before merge" },
				],
			},
		],
		workflow: "/forge:qa-setup \u2192 /forge:product design (generates AC) \u2192 /forge:qa generate \u2192 /forge:qa gate before merge.",
	},
	{
		id: "tracker",
		name: "forge-tracker",
		tier: "Pro+",
		tagline: "Your backlog lives in your tracker. Now your AI reads it too.",
		description: "forge-tracker connects your existing task management tool to the AI development pipeline. Pick a task from GitHub Issues, Linear, ClickUp, Notion, or Jira - your AI gets the full context (description, acceptance criteria, labels, priority) and starts working. When done, it syncs status and links the PR back to the task. No more copy-pasting issue descriptions into prompts.",
		benefits: [
			"5 integrations: GitHub Issues, Linear, ClickUp, Notion, Jira",
			"Pick tasks directly from your tracker - AI gets full context",
			"Auto-syncs task status and progress back to tracker",
			"Links PRs and commits to tasks automatically",
			"AC validation ensures task requirements are met before closing",
		],
		commands: [
			{
				command: "/forge:tracker-setup",
				description: "Configure task tracker integration - select provider and connect.",
				subcommands: [
					{ sub: "/forge:tracker-setup", detail: "Interactive tracker setup (auto-detects available integrations)" },
				],
			},
			{
				command: "/forge:tracker",
				description: "Task management operations - pick, sync, close tasks.",
				subcommands: [
					{ sub: "/forge:tracker pick", detail: "Pick a task from your tracker to work on" },
					{ sub: "/forge:tracker sync", detail: "Sync task status and progress back to tracker" },
					{ sub: "/forge:tracker close", detail: "Close task with summary and link to PR/commit" },
				],
			},
		],
		workflow: "/forge:tracker-setup \u2192 /forge:tracker pick \u2192 /dev \u2192 /forge:tracker sync \u2192 /forge:tracker close.",
	},
	{
		id: "prompts",
		name: "forge-prompts",
		tier: "Pro+",
		tagline: "Your AI prompts are code. Manage them like code.",
		description: "forge-prompts brings software engineering rigor to prompt management. It inventories every LLM prompt in your project, audits them against provider-specific best practices, and tracks quality over time. Three frameworks (CO-STAR, RISEN, TIDD-EC) guide structured prompt design. A dedicated subagent handles prompt writing and refactoring - so your prompts evolve as your product grows.",
		benefits: [
			"Auto-inventories all LLM prompts across your codebase",
			"Provider-aware auditing (Anthropic, OpenAI, generic)",
			"3 structured frameworks: CO-STAR, RISEN, TIDD-EC",
			"Dedicated prompt-engineer subagent for writing and refactoring",
			"Learning loop captures prompt patterns from your project",
		],
		commands: [
			{
				command: "/forge:prompts-setup",
				description: "Configure prompt architecture, principles, and provider settings.",
				subcommands: [
					{ sub: "/forge:prompts-setup", detail: "3-gate wizard: LLM detection, prompt architecture, principles" },
				],
			},
			{
				command: "/forge:prompts",
				description: "Prompt engineering operations - inventory, audit, review, test, evolve.",
				subcommands: [
					{ sub: "/forge:prompts inventory", detail: "Scan codebase and list all LLM prompts with metadata" },
					{ sub: "/forge:prompts audit", detail: "Audit prompts against provider best practices" },
					{ sub: "/forge:prompts review", detail: "Deep review of a specific prompt with improvement suggestions" },
					{ sub: "/forge:prompts test", detail: "Test prompt with sample inputs, evaluate output quality" },
					{ sub: "/forge:prompts evolve", detail: "Apply learning loop - capture patterns, update principles" },
				],
			},
		],
		workflow: "/forge:prompts-setup \u2192 /forge:prompts inventory \u2192 audit \u2192 fix issues \u2192 /forge:prompts evolve.",
	},
	{
		id: "autopilot",
		name: "forge-autopilot",
		tier: "Complete",
		tagline: "Run 3 agents in parallel. Zero merge conflicts.",
		description: "forge-autopilot orchestrates autonomous AI teammates like Kubernetes orchestrates containers. Give it a list of features - it triages them, detects potential conflicts (shared models, overlapping files), groups non-conflicting features into parallel waves, and deploys agents to isolated worktrees. Each agent follows your full pipeline (guardrails, tests, quality gates). When a wave completes, it auto-merges and starts the next wave.",
		benefits: [
			"Backlog triage classifies features by complexity and risk",
			"Conflict detection prevents agents from editing the same files",
			"Wave execution runs non-conflicting features in parallel",
			"3 autonomy modes: supervised, semi-auto, full-auto",
			"Auto-merge with quality gate verification before merge",
		],
		commands: [
			{
				command: "/forge:autopilot",
				description: "Launch batch autonomous development pipeline.",
				subcommands: [
					{ sub: "/forge:autopilot --setup", detail: "Configure autonomy mode and feature sources" },
					{ sub: "/forge:autopilot", detail: "Start batch with configured sources (triage \u2192 waves \u2192 execute)" },
					{ sub: '/forge:autopilot "auth, payments"', detail: "Start batch with specific features" },
					{ sub: "/forge:autopilot --resume", detail: "Resume interrupted batch from last checkpoint" },
					{ sub: "/forge:autopilot --abort", detail: "Abort current batch and clean up worktrees" },
				],
			},
		],
		workflow: "/forge:autopilot --setup \u2192 /forge:autopilot \u2192 agents triage, detect conflicts, execute in waves \u2192 review results.",
	},
	{
		id: "worktree",
		name: "forge-worktree",
		tier: "Starter+",
		tagline: "Isolated branches with zero port conflicts. Automatic cleanup.",
		description: "forge-worktree manages the git worktree lifecycle for parallel development. Each worktree gets its own directory, Docker port offsets, and environment configuration - no more port conflicts between branches. Three templates cover common setups: Docker backend (with port isolation), Frontend (with dev server offsets), and Generic. Worktrees are auto-provisioned by forge-autopilot or created manually.",
		benefits: [
			"3 templates: Docker backend, Frontend, Generic",
			"Automatic Docker port isolation per worktree",
			"Auto-cleanup when branches are merged",
			"Integrates with forge-autopilot for autonomous provisioning",
			"Zero manual configuration for environment isolation",
		],
		commands: [
			{
				command: "/forge:worktree-setup",
				description: "Configure worktree templates and isolation settings.",
				subcommands: [
					{ sub: "/forge:worktree-setup", detail: "Interactive worktree configuration - select template (Docker backend, Frontend, Generic)" },
				],
			},
		],
		workflow: "/forge:worktree-setup \u2192 worktrees are auto-provisioned by forge-autopilot or created manually via git worktree.",
	},
];

const advisoryModules = [
	{
		id: "discovery",
		name: "forge-discovery",
		tier: "Complete",
		tagline: "Validate before you build. Research before you commit.",
		description: "forge-discovery structures the messy early phase of product development. Instead of jumping to code, it guides you through ideation, competitive research, problem validation, and user interview planning. The output feeds directly into forge-product - so by the time you design features, you have validated assumptions, not guesses.",
		benefits: [
			"Guided ideation with structured brainstorming frameworks",
			"Competitive research with positioning analysis",
			"Problem validation before committing to solutions",
			"User interview script generation for qualitative research",
			"Discovery artifacts feed directly into forge-product",
		],
		commands: [
			{
				command: "/forge:discovery-setup",
				description: "Configure discovery scope and research methodology.",
				subcommands: [
					{ sub: "/forge:discovery-setup", detail: "Setup wizard - define scope, target market, research approach" },
				],
			},
			{
				command: "/forge:discovery",
				description: "Product discovery operations - ideate, research, validate, interview.",
				subcommands: [
					{ sub: "/forge:discovery ideate", detail: "Structured brainstorming from raw idea to opportunity map" },
					{ sub: "/forge:discovery research", detail: "Competitive analysis and market research" },
					{ sub: "/forge:discovery validate", detail: "Problem validation with evidence-based scoring" },
					{ sub: "/forge:discovery interview", detail: "Generate user interview scripts and analysis frameworks" },
				],
			},
		],
		workflow: "/forge:discovery-setup \u2192 ideate \u2192 research \u2192 validate \u2192 feed results into /forge:product.",
	},
	{
		id: "marketing",
		name: "forge-marketing",
		tier: "Complete",
		tagline: "From validated problem to sellable product. Strategy, not guesswork.",
		description: "forge-marketing turns your validated product insights into marketing strategy. It generates positioning, pricing recommendations, go-to-market plans, and content calendars - all grounded in your product data, not generic templates. When forge-discovery and forge-product are installed, it reads their artifacts automatically for context-aware recommendations.",
		benefits: [
			"Positioning framework tailored to your product and market",
			"Data-driven pricing with competitive context",
			"Go-to-market plan with channel strategy and timeline",
			"Content calendar aligned with product milestones",
			"Reads discovery and product artifacts for context-aware output",
		],
		commands: [
			{
				command: "/forge:marketing-setup",
				description: "Configure marketing scope and strategy parameters.",
				subcommands: [
					{ sub: "/forge:marketing-setup", detail: "Setup wizard - define market, channels, budget constraints" },
				],
			},
			{
				command: "/forge:marketing",
				description: "Marketing operations - positioning, pricing, GTM, content.",
				subcommands: [
					{ sub: "/forge:marketing position", detail: "Generate positioning and messaging framework" },
					{ sub: "/forge:marketing price", detail: "Pricing strategy with tier design and competitive analysis" },
					{ sub: "/forge:marketing launch", detail: "Go-to-market plan with timeline and channel strategy" },
					{ sub: "/forge:marketing content", detail: "Content calendar aligned with product roadmap" },
				],
			},
		],
		workflow: "/forge:marketing-setup \u2192 position \u2192 price \u2192 launch plan \u2192 content calendar.",
	},
	{
		id: "copy",
		name: "forge-copy",
		tier: "Complete",
		tagline: "Consistent voice across every screen, email, and notification.",
		description: "forge-copy manages production copy as a first-class concern. Define your brand voice once - it generates and reviews UI microcopy, email templates, notification text, and error messages against your voice guidelines. When forge-product or forge-marketing are installed, it uses their positioning and user research for copy that speaks your customer's language.",
		benefits: [
			"Brand voice configuration with tone and style rules",
			"Generates UI microcopy, emails, notifications, error messages",
			"Reviews existing copy against brand guidelines",
			"Uses product and marketing context for customer-aware copy",
			"Consistency checks across all copy touchpoints",
		],
		commands: [
			{
				command: "/forge:copy-setup",
				description: "Configure brand voice, tone rules, and copy scope.",
				subcommands: [
					{ sub: "/forge:copy-setup", detail: "Setup wizard - define voice, tone, terminology, style rules" },
				],
			},
			{
				command: "/forge:copy",
				description: "Copy operations - generate and review across all contexts.",
				subcommands: [
					{ sub: "/forge:copy generate", detail: "Generate copy for a specific context (UI, email, notification)" },
					{ sub: "/forge:copy review", detail: "Review existing copy against brand voice guidelines" },
				],
			},
		],
		workflow: "/forge:copy-setup \u2192 /forge:copy generate for each context \u2192 /forge:copy review before release.",
	},
	{
		id: "seo",
		name: "forge-seo",
		tier: "Complete",
		tagline: "Technical SEO your AI actually understands. Not a checklist - a strategy.",
		description: "forge-seo combines technical SEO auditing with keyword research and schema markup generation. It scans your pages for issues (missing meta tags, broken structured data, performance bottlenecks), researches keywords aligned with your product positioning, and generates JSON-LD schema markup. When forge-analytics is installed, it correlates SEO metrics with user behavior data.",
		benefits: [
			"Technical SEO audit with prioritized fix list",
			"Keyword research aligned with product positioning",
			"JSON-LD schema markup generation for rich snippets",
			"Content gap analysis based on competitor rankings",
			"Correlates with analytics data when forge-analytics is installed",
		],
		commands: [
			{
				command: "/forge:seo-setup",
				description: "Configure SEO scope and target parameters.",
				subcommands: [
					{ sub: "/forge:seo-setup", detail: "Setup wizard - define target pages, keywords, competitors" },
				],
			},
			{
				command: "/forge:seo",
				description: "SEO operations - audit, keywords, schema markup.",
				subcommands: [
					{ sub: "/forge:seo audit", detail: "Technical SEO audit with prioritized issue list" },
					{ sub: "/forge:seo keywords", detail: "Keyword research with search intent analysis" },
					{ sub: "/forge:seo schema", detail: "Generate JSON-LD structured data markup" },
				],
			},
		],
		workflow: "/forge:seo-setup \u2192 /forge:seo audit \u2192 fix issues \u2192 keywords \u2192 schema.",
	},
	{
		id: "analytics",
		name: "forge-analytics",
		tier: "Complete",
		tagline: "Measurement-first. Plan what to track before you ship.",
		description: "forge-analytics flips the usual approach: instead of adding tracking after launch, you define a measurement plan during development. It generates event schemas, tracking plans, and dashboard specifications before a single line of tracking code is written. Your team knows exactly what data you'll have on day one.",
		benefits: [
			"Measurement-first tracking plans defined during development",
			"Event schema generation with naming conventions",
			"Dashboard specifications ready for implementation",
			"Aligned with product artifacts (user flows, acceptance criteria)",
			"No post-launch tracking gaps - plan before you ship",
		],
		commands: [
			{
				command: "/forge:analytics-setup",
				description: "Configure analytics scope and measurement strategy.",
				subcommands: [
					{ sub: "/forge:analytics-setup", detail: "Setup wizard - define key metrics, events, data sources" },
				],
			},
			{
				command: "/forge:analytics",
				description: "Analytics operations - tracking plans, event schemas, dashboards.",
				subcommands: [
					{ sub: "/forge:analytics plan", detail: "Generate measurement plan with KPIs and event taxonomy" },
					{ sub: "/forge:analytics schema", detail: "Generate event schemas with naming conventions" },
					{ sub: "/forge:analytics dashboard", detail: "Design dashboard specifications for key metrics" },
				],
			},
		],
		workflow: "/forge:analytics-setup \u2192 plan \u2192 schema \u2192 implement tracking \u2192 dashboard.",
	},
	{
		id: "onboarding",
		name: "forge-onboarding",
		tier: "Complete",
		tagline: "From signup to aha moment. Design the path, not just the page.",
		description: "forge-onboarding designs activation flows that convert signups into engaged users. It maps the journey from first login to the \"aha moment\" - the point where users experience core value. Generates step-by-step onboarding flows, empty state designs, tooltip sequences, and progress indicators. When forge-product is installed, it uses user flows and personas to tailor the onboarding path.",
		benefits: [
			"Activation flow design from signup to aha moment",
			"Empty state and first-run experience specifications",
			"Tooltip and guided tour sequence planning",
			"Progress indicator and milestone design",
			"Audit existing onboarding for drop-off points",
		],
		commands: [
			{
				command: "/forge:onboarding-setup",
				description: "Configure onboarding scope and activation goals.",
				subcommands: [
					{ sub: "/forge:onboarding-setup", detail: "Setup wizard - define aha moment, activation steps, personas" },
				],
			},
			{
				command: "/forge:onboarding",
				description: "Onboarding design operations - design flows, audit existing experience.",
				subcommands: [
					{ sub: "/forge:onboarding design", detail: "Design activation flow from signup to aha moment" },
					{ sub: "/forge:onboarding audit", detail: "Audit existing onboarding for friction and drop-off points" },
				],
			},
		],
		workflow: "/forge:onboarding-setup \u2192 design \u2192 implement \u2192 /forge:onboarding audit after launch.",
	},
	{
		id: "growth",
		name: "forge-growth",
		tier: "Complete",
		tagline: "Retain, refer, expand. Three levers, one module.",
		description: "forge-growth structures the three core growth levers: retention (reduce churn), referral (organic acquisition), and expansion (upsell and cross-sell). It generates churn prevention strategies with cancel flow designs, referral program frameworks, and expansion revenue playbooks. When forge-analytics is installed, it grounds recommendations in actual user behavior data.",
		benefits: [
			"Churn prevention with cancel flow and save offer design",
			"Referral program framework with incentive modeling",
			"Expansion revenue playbook (upsell, cross-sell triggers)",
			"Grounded in analytics data when forge-analytics is installed",
			"Cohort-based retention analysis recommendations",
		],
		commands: [
			{
				command: "/forge:growth-setup",
				description: "Configure growth scope and retention goals.",
				subcommands: [
					{ sub: "/forge:growth-setup", detail: "Setup wizard - define growth model, retention targets, referral goals" },
				],
			},
			{
				command: "/forge:growth",
				description: "Growth operations - retention, referral, expansion strategies.",
				subcommands: [
					{ sub: "/forge:growth retain", detail: "Churn prevention strategy with cancel flow design" },
					{ sub: "/forge:growth refer", detail: "Referral program design with incentive structure" },
					{ sub: "/forge:growth expand", detail: "Expansion revenue playbook (upsell, cross-sell)" },
				],
			},
		],
		workflow: "/forge:growth-setup \u2192 retain \u2192 refer \u2192 expand.",
	},
	{
		id: "ab",
		name: "forge-ab",
		tier: "Complete",
		tagline: "Statistically valid experiments. Not \"we changed the button color.\"",
		description: "forge-ab brings statistical rigor to product experimentation. It structures experiments from hypothesis to analysis: defines success metrics, calculates required sample sizes, designs variants, and provides analysis frameworks. No more running tests without enough traffic, or declaring winners based on gut feeling. When forge-analytics is installed, it uses your event schemas for metric definitions.",
		benefits: [
			"Structured hypothesis format with measurable outcomes",
			"Sample size calculation to avoid underpowered tests",
			"Experiment design with variant specifications",
			"Statistical analysis framework for interpreting results",
			"Uses analytics event schemas for consistent metrics",
		],
		commands: [
			{
				command: "/forge:ab-setup",
				description: "Configure experiment tracking and analysis settings.",
				subcommands: [
					{ sub: "/forge:ab-setup", detail: "Setup wizard - experiment infrastructure, significance thresholds" },
				],
			},
			{
				command: "/forge:ab",
				description: "A/B experiment operations - hypothesis, design, analyze.",
				subcommands: [
					{ sub: "/forge:ab hypothesis", detail: "Structure experiment hypothesis with success criteria" },
					{ sub: "/forge:ab design", detail: "Design experiment with variants, metrics, sample size" },
					{ sub: "/forge:ab analyze", detail: "Analyze results with statistical significance testing" },
				],
			},
		],
		workflow: "/forge:ab-setup \u2192 hypothesis \u2192 design \u2192 run experiment \u2192 analyze.",
	},
];

const modules = [...coreModules, ...advisoryModules];
---

<DocsLayout
	title="Usage Guide - Forge DevKit"
	description="Complete command reference and module guide for all 15 Forge DevKit modules."
>
	<h1 class="font-sans text-[36px] md:text-[48px] font-black leading-[1.1] tracking-[-0.02em] text-text mb-4">
		Usage Guide
	</h1>
	<p class="font-sans text-[18px] md:text-[20px] text-text-secondary leading-[1.6] mb-6">
		Complete guide to all 15 Forge DevKit modules &mdash; what they do, how to use them, and why they matter.
	</p>
	<p class="text-[15px] text-text-muted leading-[1.7] mb-16">
		All commands run inside your AI session (Claude Code, Cursor, or Claude Desktop). Forge DevKit is a plugin system &mdash; commands become available after <a href="/docs/getting-started" class="text-secondary hover:underline">installation</a>.
	</p>

	{/* Quick nav - Core Pipeline */}
	<nav class="mb-6 rounded-[var(--radius-card)] border border-card bg-surface-card p-6">
		<p class="font-mono text-[12px] font-medium uppercase tracking-[0.1em] text-text-muted mb-4">Core Pipeline</p>
		<div class="flex flex-wrap gap-3">
			{coreModules.map((m) => (
				<a href={`#${m.id}`} class="inline-flex items-center gap-2 rounded-full border border-card px-4 py-1.5 font-mono text-[13px] text-text hover:border-secondary/40 hover:text-secondary transition-colors">
					{m.name}
					<span class="text-[11px] text-text-muted">{m.tier}</span>
				</a>
			))}
		</div>
	</nav>

	{/* Quick nav - Advisory Modules */}
	<nav class="mb-16 rounded-[var(--radius-card)] border border-card bg-surface-card p-6">
		<p class="font-mono text-[12px] font-medium uppercase tracking-[0.1em] text-text-muted mb-4">Advisory Modules</p>
		<div class="flex flex-wrap gap-3">
			{advisoryModules.map((m) => (
				<a href={`#${m.id}`} class="inline-flex items-center gap-2 rounded-full border border-card px-4 py-1.5 font-mono text-[13px] text-text hover:border-secondary/40 hover:text-secondary transition-colors">
					{m.name}
					<span class="text-[11px] text-text-muted">{m.tier}</span>
				</a>
			))}
		</div>
	</nav>

	{/* --- Core Pipeline Modules --- */}
	<div class="mb-8">
		<div class="flex items-center gap-4 mb-2">
			<h2 class="font-sans text-[22px] md:text-[26px] font-bold text-text">Core Pipeline</h2>
			<span class="text-[13px] text-text-muted">{coreModules.length} modules</span>
		</div>
		<p class="text-[15px] text-text-muted leading-[1.7]">
			The development backbone. These modules power the setup wizard, development pipeline, testing, task tracking, parallel execution, and branch isolation.
		</p>
	</div>

	{coreModules.map((m) => (
		<section id={m.id} class="mb-20 scroll-mt-24">
			<div class="flex items-baseline gap-3 mb-2">
				<h3 class="font-sans text-[28px] md:text-[32px] font-bold text-text">
					{m.name}
				</h3>
				<span class="rounded-full bg-[rgba(118,73,196,0.08)] px-3 py-0.5 font-mono text-[12px] text-secondary">
					{m.tier}
				</span>
			</div>

			<p class="font-sans text-[18px] text-text leading-[1.5] font-medium mb-4">
				{m.tagline}
			</p>

			<p class="text-[15px] text-text-secondary leading-[1.7] mb-2">
				{m.description}
			</p>

			{m.id === "core" && (
				<>
					<DocHighlight variant="pain">
						<strong>Without Forge:</strong> you spend 20 minutes every session re-explaining your architecture to AI. It forgets your NestJS guards, your layer conventions, your naming patterns - every single time.
					</DocHighlight>
					<DocHighlight variant="gain">
						<strong>With forge-core:</strong> one <code>/forge:setup</code> and your AI permanently knows your stack, layers, and conventions. No manual prompt engineering. No re-explaining. Ever.
					</DocHighlight>
				</>
			)}
			{m.id === "product" && (
				<>
					<DocHighlight variant="pain">
						<strong>Without Forge:</strong> you describe a feature vaguely, AI hallucinates requirements, builds the wrong thing, and you spend hours debugging assumptions that were never verified.
					</DocHighlight>
					<DocHighlight variant="gain">
						<strong>With forge-product:</strong> describe features in business language - AI generates verified user flows, stories, and acceptance criteria <em>before</em> writing a single line of code.
					</DocHighlight>
				</>
			)}
			{m.id === "qa" && (
				<>
					<DocHighlight variant="pain">
						<strong>Without Forge:</strong> AI generates tests with 100% coverage - all using mock data. Every assertion tests implementation details. Zero real behavior verified.
					</DocHighlight>
					<DocHighlight variant="gain">
						<strong>With forge-qa:</strong> every test traces back to an acceptance criterion. No orphan tests. No missing coverage. Tests prove requirements, not AI guesswork.
					</DocHighlight>
				</>
			)}
			{m.id === "tracker" && (
				<>
					<DocHighlight variant="pain">
						<strong>Without Forge:</strong> you copy-paste issue descriptions from your tracker into AI prompts. Context gets lost. Status updates are manual. PRs never link back to tasks.
					</DocHighlight>
					<DocHighlight variant="gain">
						<strong>With forge-tracker:</strong> <code>/forge:tracker pick</code> pulls the task with full context. When you're done, <code>/forge:tracker close</code> syncs status and links the PR - zero manual updates.
					</DocHighlight>
				</>
			)}
			{m.id === "prompts" && (
				<DocHighlight variant="insight">
					<strong>Why manage prompts?</strong> AI-powered apps ship dozens of LLM prompts. Without inventory and auditing, prompts rot as your codebase evolves - leading to degraded AI output that's hard to trace back to a stale system prompt.
				</DocHighlight>
			)}
			{m.id === "autopilot" && (
				<>
					<DocHighlight variant="pain">
						<strong>Without Forge:</strong> you run 3 agents in parallel - they all edit the same User model. You spend 4 hours resolving merge conflicts. Net productivity: <em>negative</em>.
					</DocHighlight>
					<DocHighlight variant="gain">
						<strong>With forge-autopilot:</strong> conflict detection groups features into safe waves. Each agent gets an isolated worktree with your full pipeline. Auto-merge after quality gates pass.
					</DocHighlight>
				</>
			)}
			{m.id === "worktree" && (
				<DocHighlight variant="insight">
					<strong>Why worktrees?</strong> Each Agent Teams teammate needs isolation - its own branch, ports, and environment. Manual setup is error-prone. forge-worktree automates the entire lifecycle, so you focus on features, not infrastructure.
				</DocHighlight>
			)}

			<div class="rounded-[var(--radius-card)] border border-card bg-surface-card p-5 mb-8">
				<p class="font-mono text-[12px] font-medium uppercase tracking-[0.1em] text-text-muted mb-3">What you get</p>
				<ul class="space-y-2">
					{m.benefits.map((b) => (
						<li class="flex items-start gap-2 text-[14px] text-text-secondary leading-[1.6]">
							<span class="text-secondary mt-0.5 shrink-0">&#10003;</span>
							<span>{b}</span>
						</li>
					))}
				</ul>
			</div>

			<div class="space-y-6 mb-6">
				{m.commands.map((cmd) => (
					<div class="rounded-[var(--radius-card)] border border-card bg-surface-card overflow-hidden">
						<div class="border-b border-card px-5 py-3 flex items-baseline gap-3">
							<code class="font-mono text-[15px] font-medium text-secondary">{cmd.command}</code>
							<span class="text-[13px] text-text-muted">{cmd.description}</span>
						</div>
						<div class="px-5 py-3">
							<table class="w-full">
								<tbody>
									{cmd.subcommands.map((sc) => (
										<tr class="border-b border-card/30 last:border-0">
											<td class="py-2 pr-4 font-mono text-[13px] text-text whitespace-nowrap align-top">{sc.sub}</td>
											<td class="py-2 text-[13px] text-text-muted">{sc.detail}</td>
										</tr>
									))}
								</tbody>
							</table>
						</div>
					</div>
				))}
			</div>

			<div class="rounded-[var(--radius-card)] border border-secondary/15 bg-secondary/5 px-5 py-3">
				<p class="text-[13px]">
					<span class="font-mono text-[12px] font-medium text-secondary mr-2">Typical workflow:</span>
					<span class="text-text-muted">{m.workflow}</span>
				</p>
			</div>
		</section>
	))}

	{/* --- Advisory Modules --- */}
	<div class="mb-8 mt-8 pt-8 border-t border-[rgba(118,73,196,0.15)]">
		<div class="flex items-center gap-4 mb-2">
			<h2 class="font-sans text-[22px] md:text-[26px] font-bold text-text">Advisory Modules</h2>
			<span class="text-[13px] text-text-muted">{advisoryModules.length} modules</span>
		</div>
		<p class="text-[15px] text-text-muted leading-[1.7] mb-2">
			Strategic advisors that surround the core pipeline. Each module generates planning artifacts, audits, and recommendations - from discovery and marketing to SEO, analytics, and growth. Available in the Complete plan.
		</p>
		<DocHighlight variant="insight">
			<strong>How advisory modules work:</strong> They read artifacts from the core pipeline (user flows, acceptance criteria, product strategy) and generate domain-specific outputs. Each module connects to others through shared state - forge-marketing reads forge-discovery results, forge-copy reads forge-marketing positioning, and so on. Install one or install all - each works standalone but gets smarter with context from its neighbors.
		</DocHighlight>
	</div>

	{advisoryModules.map((m) => (
		<section id={m.id} class="mb-20 scroll-mt-24">
			<div class="flex items-baseline gap-3 mb-2">
				<h3 class="font-sans text-[28px] md:text-[32px] font-bold text-text">
					{m.name}
				</h3>
				<span class="rounded-full bg-[rgba(118,73,196,0.08)] px-3 py-0.5 font-mono text-[12px] text-secondary">
					{m.tier}
				</span>
			</div>

			<p class="font-sans text-[18px] text-text leading-[1.5] font-medium mb-4">
				{m.tagline}
			</p>

			<p class="text-[15px] text-text-secondary leading-[1.7] mb-6">
				{m.description}
			</p>

			<div class="rounded-[var(--radius-card)] border border-card bg-surface-card p-5 mb-8">
				<p class="font-mono text-[12px] font-medium uppercase tracking-[0.1em] text-text-muted mb-3">What you get</p>
				<ul class="space-y-2">
					{m.benefits.map((b) => (
						<li class="flex items-start gap-2 text-[14px] text-text-secondary leading-[1.6]">
							<span class="text-secondary mt-0.5 shrink-0">&#10003;</span>
							<span>{b}</span>
						</li>
					))}
				</ul>
			</div>

			<div class="space-y-6 mb-6">
				{m.commands.map((cmd) => (
					<div class="rounded-[var(--radius-card)] border border-card bg-surface-card overflow-hidden">
						<div class="border-b border-card px-5 py-3 flex items-baseline gap-3">
							<code class="font-mono text-[15px] font-medium text-secondary">{cmd.command}</code>
							<span class="text-[13px] text-text-muted">{cmd.description}</span>
						</div>
						<div class="px-5 py-3">
							<table class="w-full">
								<tbody>
									{cmd.subcommands.map((sc) => (
										<tr class="border-b border-card/30 last:border-0">
											<td class="py-2 pr-4 font-mono text-[13px] text-text whitespace-nowrap align-top">{sc.sub}</td>
											<td class="py-2 text-[13px] text-text-muted">{sc.detail}</td>
										</tr>
									))}
								</tbody>
							</table>
						</div>
					</div>
				))}
			</div>

			<div class="rounded-[var(--radius-card)] border border-secondary/15 bg-secondary/5 px-5 py-3">
				<p class="text-[13px]">
					<span class="font-mono text-[12px] font-medium text-secondary mr-2">Typical workflow:</span>
					<span class="text-text-muted">{m.workflow}</span>
				</p>
			</div>
		</section>
	))}

	{/* Feedback & Bug Reports */}
	<section id="feedback" class="mb-16 scroll-mt-24">
		<h2 class="font-sans text-[28px] md:text-[32px] font-bold text-text mb-3">
			Feedback & Bug Reports
		</h2>
		<p class="text-[16px] text-text-secondary leading-[1.7] mb-6">
			Found a bug? Have a suggestion? Use the built-in feedback command &mdash; it sends structured reports directly to the Forge team.
		</p>

		<div class="rounded-[var(--radius-card)] border border-card bg-surface-card overflow-hidden mb-6">
			<div class="border-b border-card px-5 py-3 flex items-baseline gap-3">
				<code class="font-mono text-[15px] font-medium text-secondary">/forge:feedback</code>
				<span class="text-[13px] text-text-muted">Send feedback, bug reports, or feature requests.</span>
			</div>
			<div class="px-5 py-3">
				<table class="w-full">
					<tbody>
						<tr class="border-b border-card/30">
							<td class="py-2 pr-4 font-mono text-[13px] text-text whitespace-nowrap align-top">/forge:feedback</td>
							<td class="py-2 text-[13px] text-text-muted">Interactive feedback wizard &mdash; select type (bug, friction, suggestion), describe the issue, auto-attaches session context</td>
						</tr>
					</tbody>
				</table>
			</div>
		</div>

		<div class="space-y-4 text-[15px] text-text-secondary leading-[1.7]">
			<p>
				The feedback command collects:
			</p>
			<ul class="list-disc pl-6 space-y-1">
				<li><strong class="text-text">Your description</strong> &mdash; what happened, what you expected</li>
				<li><strong class="text-text">Session context</strong> &mdash; plugin versions, detected stack, execution mode (auto-attached, no manual work)</li>
				<li><strong class="text-text">Feedback type</strong> &mdash; friction (something was hard), bug (something broke), suggestion (feature request)</li>
			</ul>
			<p>
				Reports are sent securely using your license key. No source code or file contents are included &mdash; only metadata and your description.
			</p>
			<p>
				Alternatively, email <a href="mailto:support@reumbra.com" class="text-secondary hover:underline">support@reumbra.com</a> for direct support.
			</p>
		</div>
	</section>

	{/* CTA */}
	<section class="mb-16">
		<div class="rounded-[var(--radius-card)] border border-secondary/20 bg-secondary/5 p-8 text-center">
			<p class="font-sans text-[20px] md:text-[24px] font-bold text-text mb-3">
				Ready to get started?
			</p>
			<p class="text-[16px] text-text-secondary mb-6">
				One-time purchase. 14-day money-back guarantee.
			</p>
			<div class="flex flex-col sm:flex-row items-center justify-center gap-4">
				<a
					href="/pricing"
					class="inline-flex items-center justify-center rounded-[var(--radius-button)] bg-primary px-8 py-3 font-sans text-[15px] font-medium text-white transition-all duration-200 hover:bg-primary-hover"
				>
					Get Forge &mdash; from &euro;29 &rarr;
				</a>
				<a
					href="/docs/getting-started"
					class="inline-flex items-center justify-center rounded-[var(--radius-button)] border border-card px-8 py-3 font-sans text-[15px] font-medium text-text transition-all duration-200 hover:border-secondary/40"
				>
					Installation guide
				</a>
			</div>
		</div>
	</section>
</DocsLayout>
